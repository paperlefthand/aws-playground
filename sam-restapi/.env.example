# Copy to `.env` and fill in environment-specific values.
# `.env` is gitignored.

# AWS named profile used by SAM CLI for credential resolution.
AWS_PROFILE=

# S3 bucket used by `sam deploy` to upload built artifacts.
SAM_S3_BUCKET=
